critical = irandom_range(min_critical, max_critical);
stopping_power = irandom_range(min_stopping, max_stopping);
create_hitscan(x + (image_xscale * scopex), y - scopey, room_width, 0, self, 0);           

